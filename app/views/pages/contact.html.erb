<% content_for :title, "문의하기 - 디어스" %>
<% content_for :description, "프로젝트 문의 및 견적 요청을 남겨주세요. 디어스 전문가가 빠르게 연락드리겠습니다." %>

<!-- Contact Hero -->
<section class="hero" style="min-height: 40vh; padding-top: 120px;">
  <div class="hero-content">
    <h1 class="hero-title" style="font-size: var(--text-6xl);">프로젝트 문의</h1>
    <p class="hero-subtitle">
      프로젝트에 대해 자세히 알려주세요<br>
      최적의 솔루션을 제안해드리겠습니다
    </p>
  </div>
</section>

<!-- Contact Form Section -->
<section class="section">
  <div class="container" style="max-width: 800px;">
    <% if notice %>
      <div style="background: var(--color-success); color: white; padding: var(--space-4) var(--space-6); border-radius: var(--radius-lg); margin-bottom: var(--space-8); display: flex; align-items: center; gap: var(--space-3);">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
          <polyline points="22 4 12 14.01 9 11.01"/>
        </svg>
        <span><%= notice %></span>
      </div>
    <% end %>

    <%= form_with model: @quote, url: contact_path, method: :post, class: "card", style: "padding: var(--space-8);" do |f| %>
      <% content_for :title, "프로젝트 문의 - 디어스" %>

<!-- 상단 네비게이션과 겹치지 않도록 여백 추가 -->
<div style="height: 80px;"></div>

<% if params[:template_id].present? %>
  <!-- [템플릿 의뢰 모드] 와이드 분할 레이아웃 (96% Width) -->
  <div style="width: 96%; max-width: 1920px; margin: 0 auto; padding-bottom: 80px;">
    
    <div style="margin-bottom: 20px;">
      <h1 class="text-3xl font-bold">
        <span class="text-emerald-500"><%= params[:template_title] %></span> 템플릿 의뢰하기
      </h1>
      <p class="text-gray-500 mt-1">선택하신 디자인을 기반으로 빠르고 정확하게 제작해 드립니다.</p>
    </div>

    <div class="grid lg:grid-cols-2 gap-8" style="min-height: 800px;">
      
      <!-- Left: Interactive Preview -->
      <div class="bg-gray-100 rounded-2xl overflow-hidden border border-gray-200 flex flex-col relative" style="height: 85vh; position: sticky; top: 100px;">
        <!-- Preview Header -->
        <div class="bg-white border-b border-gray-200 px-4 py-3 flex justify-between items-center">
          <div class="font-bold text-gray-700 flex items-center gap-2">
            <span class="w-2 h-2 rounded-full bg-red-500"></span>
            <span class="w-2 h-2 rounded-full bg-yellow-500"></span>
            <span class="w-2 h-2 rounded-full bg-green-500"></span>
            <span class="ml-2 text-sm">Live Preview</span>
          </div>
          
          <!-- View Switcher -->
          <div class="bg-gray-100 p-1 rounded-lg flex text-sm font-medium">
            <button onclick="switchEmbedView('pc')" id="btn-embed-pc" class="px-3 py-1.5 rounded-md bg-white shadow-sm text-gray-900 transition-all">
              🖥️ PC
            </button>
            <button onclick="switchEmbedView('mobile')" id="btn-embed-mobile" class="px-3 py-1.5 rounded-md text-gray-500 hover:text-gray-900 transition-all">
              📱 Mobile
            </button>
          </div>
        </div>

        <!-- Preview Area -->
        <div class="flex-1 bg-gray-50 overflow-hidden flex justify-center items-center p-4" id="embed-container">
          <!-- Iframe will be injected here -->
          <iframe id="embed-iframe" src="" class="w-full h-full bg-white shadow-lg rounded-lg transition-all duration-500" style="border: none;"></iframe>
        </div>
      </div>

      <!-- Right: Contact Form -->
      <div class="bg-white p-8 lg:p-12 rounded-2xl border border-gray-100 shadow-xl">
        <h2 class="text-2xl font-bold mb-6">프로젝트 상세 정보</h2>
        
        <%= form_with url: contact_path, local: true, class: "space-y-6" do |f| %>
          <div class="grid grid-cols-2 gap-6">
            <div>
              <label class="block text-sm font-semibold text-gray-700 mb-2">담당자명 *</label>
              <%= f.text_field :name, class: "w-full px-4 py-3 rounded-lg bg-gray-50 border border-gray-200 focus:border-emerald-500 focus:ring-2 focus:ring-emerald-200 transition outline-none", placeholder: "홍길동" %>
            </div>
            <div>
              <label class="block text-sm font-semibold text-gray-700 mb-2">회사명</label>
              <%= f.text_field :company, class: "w-full px-4 py-3 rounded-lg bg-gray-50 border border-gray-200 focus:border-emerald-500 focus:ring-2 focus:ring-emerald-200 transition outline-none", placeholder: "주식회사 디어스" %>
            </div>
          </div>

          <div class="grid grid-cols-2 gap-6">
            <div>
              <label class="block text-sm font-semibold text-gray-700 mb-2">이메일 *</label>
              <%= f.email_field :email, class: "w-full px-4 py-3 rounded-lg bg-gray-50 border border-gray-200 focus:border-emerald-500 focus:ring-2 focus:ring-emerald-200 transition outline-none", placeholder: "contact@example.com" %>
            </div>
            <div>
              <label class="block text-sm font-semibold text-gray-700 mb-2">연락처 *</label>
              <%= f.text_field :phone, class: "w-full px-4 py-3 rounded-lg bg-gray-50 border border-gray-200 focus:border-emerald-500 focus:ring-2 focus:ring-emerald-200 transition outline-none", placeholder: "010-1234-5678" %>
            </div>
          </div>

          <div>
             <label class="block text-sm font-semibold text-gray-700 mb-2">예산 범위</label>
             <%= f.select :budget, ["500만원 ~ 1,000만원", "1,000만원 ~ 3,000만원", "3,000만원 ~ 5,000만원", "5,000만원 이상", "미정"], { prompt: "예산을 선택해주세요" }, class: "w-full px-4 py-3 rounded-lg bg-gray-50 border border-gray-200 focus:border-emerald-500 focus:ring-2 focus:ring-emerald-200 transition outline-none" %>
          </div>

          <div>
            <label class="block text-sm font-semibold text-gray-700 mb-2">요청사항 *</label>
            <% 
              default_msg = if params[:template_title]
                "안녕하세요. '#{params[:template_title]}' 템플릿을 기반으로 의뢰하고 싶습니다.\n\n구체적인 변경 요청사항:\n1. \n2. "
              else
                ""
              end
            %>
            <%= f.text_area :message, value: default_msg, rows: 8, class: "w-full px-4 py-3 rounded-lg bg-gray-50 border border-gray-200 focus:border-emerald-500 focus:ring-2 focus:ring-emerald-200 transition outline-none font-mono text-sm leading-relaxed" %>
          </div>

          <button type="submit" class="w-full py-4 bg-emerald-500 hover:bg-emerald-600 text-white font-bold rounded-xl shadow-lg shadow-emerald-200 transition transform hover:-translate-y-0.5">
            무료 견적 신청하기
          </button>
          
          <p class="text-xs text-center text-gray-400 mt-4">
            보내주신 내용은 24시간 이내에 담당자가 검토 후 연락드립니다.
          </p>
        <% end %>
      </div>
    </div>
  </div>

  <script>
    // Template Mapping Logic
    const templateId = "<%= params[:template_id] %>";
    const templateMap = {
      'beauty': '/templates/beauty',
      'dining': '/templates/dining',
      'gym': '/templates/gym',
      'study': '/templates/study',
      'stay': '/templates/stay',
      'corporate': '/templates/corporate',
      'ecommerce': '/templates/ecommerce',
      'cafe': '/templates/cafe',
      'portfolio': '/templates/portfolio',
      'medical': '/templates/medical',
      'law': '/templates/law',
      'wedding': '/templates/wedding'
    };

    const targetUrl = templateMap[templateId] || '/templates/beauty'; // Fallback
    const iframe = document.getElementById('embed-iframe');
    iframe.src = targetUrl;

    function switchEmbedView(mode) {
        const iframe = document.getElementById('embed-iframe');
        const btnPc = document.getElementById('btn-embed-pc');
        const btnMobile = document.getElementById('btn-embed-mobile');

        if (mode === 'pc') {
            iframe.style.maxWidth = '100%';
            iframe.style.height = '100%';
            iframe.style.borderRadius = '8px';
            iframe.style.border = 'none';
            btnPc.classList.add('bg-white', 'shadow-sm', 'text-gray-900');
            btnPc.classList.remove('text-gray-500');
            btnMobile.classList.remove('bg-white', 'shadow-sm', 'text-gray-900');
            btnMobile.classList.add('text-gray-500');
        } else {
            iframe.style.maxWidth = '375px';
            iframe.style.height = '812px'; // iPhone size
            iframe.style.borderRadius = '40px';
            iframe.style.border = '12px solid #333';
            btnMobile.classList.add('bg-white', 'shadow-sm', 'text-gray-900');
            btnMobile.classList.remove('text-gray-500');
            btnPc.classList.remove('bg-white', 'shadow-sm', 'text-gray-900');
            btnPc.classList.add('text-gray-500');
        }
    }
  </script>

<% else %>
  <!-- [일반 문의 모드] 기존 중앙 정렬 (Container) -->
  <div class="container pb-20">
    <div class="max-w-3xl mx-auto bg-white p-8 md:p-12 rounded-3xl shadow-2xl border border-gray-100">
      <div class="text-center mb-10">
        <h1 class="text-4xl font-bold mb-3 text-gray-900">프로젝트 문의</h1>
        <p class="text-gray-500">
          성공적인 프로젝트의 시작, 디어스가 함께하겠습니다.
        </p>
      </div>

      <%= form_with url: contact_path, local: true, class: "space-y-6" do |f| %>
        <!-- (기존 폼 내용 유지) -->
        <div class="grid md:grid-cols-2 gap-6">
          <div>
            <label class="block text-sm font-bold text-gray-700 mb-2">이름 *</label>
            <%= f.text_field :name, class: "w-full border-gray-300 rounded-lg shadow-sm focus:border-indigo-500 focus:ring-indigo-500 py-3 px-4", required: true %>
          </div>
          <div>
            <label class="block text-sm font-bold text-gray-700 mb-2">회사명</label>
            <%= f.text_field :company, class: "w-full border-gray-300 rounded-lg shadow-sm focus:border-indigo-500 focus:ring-indigo-500 py-3 px-4" %>
          </div>
        </div>

        <div class="grid md:grid-cols-2 gap-6">
          <div>
            <label class="block text-sm font-bold text-gray-700 mb-2">이메일 *</label>
            <%= f.email_field :email, class: "w-full border-gray-300 rounded-lg shadow-sm focus:border-indigo-500 focus:ring-indigo-500 py-3 px-4", required: true %>
          </div>
          <div>
            <label class="block text-sm font-bold text-gray-700 mb-2">연락처 *</label>
            <%= f.text_field :phone, class: "w-full border-gray-300 rounded-lg shadow-sm focus:border-indigo-500 focus:ring-indigo-500 py-3 px-4", required: true %>
          </div>
        </div>

        <div>
          <label class="block text-sm font-bold text-gray-700 mb-2">프로젝트 유형 *</label>
          <%= f.select :project_type, ["웹사이트 개발", "애플리케이션 개발", "쇼핑몰 구축", "유지보수", "기타"], { prompt: "선택해주세요" }, class: "w-full border-gray-300 rounded-lg shadow-sm focus:border-indigo-500 focus:ring-indigo-500 py-3 px-4", required: true %>
        </div>

        <div>
           <label class="block text-sm font-bold text-gray-700 mb-2">예산 범위</label>
           <%= f.select :budget, ["500만원 ~ 1,000만원", "1,000만원 ~ 3,000만원", "3,000만원 ~ 5,000만원", "5,000만원 이상", "미정"], { prompt: "선택해주세요" }, class: "w-full border-gray-300 rounded-lg shadow-sm focus:border-indigo-500 focus:ring-indigo-500 py-3 px-4" %>
        </div>

        <div>
          <label class="block text-sm font-bold text-gray-700 mb-2">문의 내용 *</label>
          <%= f.text_area :message, rows: 6, class: "w-full border-gray-300 rounded-lg shadow-sm focus:border-indigo-500 focus:ring-indigo-500 py-3 px-4", required: true, placeholder: "프로젝트의 목적, 주요 기능, 일정 등을 자유롭게 적어주세요." %>
        </div>

        <div class="pt-4">
          <button type="submit" class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-4 rounded-xl shadow-lg transition duration-200 flex justify-center items-center gap-2">
            <span>문의하기</span>
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"></path></svg>
          </button>
        </div>
      <% end %>
    </div>
  </div>
<% end %>
      <div style="margin-top: var(--space-8);">
        <%= f.submit "문의하기", class: "btn btn-primary btn-lg", style: "width: 100%;" %>
      </div>

      <p style="text-align: center; color: var(--color-gray-500); font-size: var(--text-sm); margin-top: var(--space-4);">
        문의 주신 내용은 영업일 기준 24시간 이내에 답변드립니다.
      </p>
    <% end %>
  </div>
</section>

<!-- Contact Info Section -->
<section class="section bg-gray-50">
  <div class="container">
    <div class="text-center" style="margin-bottom: var(--space-12);">
      <h2 style="font-size: var(--text-4xl); margin-bottom: var(--space-4);">다른 방법으로 연락하기</h2>
      <p class="text-gray" style="font-size: var(--text-lg);">
        편하신 방법으로 언제든지 연락주세요
      </p>
    </div>

    <div class="grid grid-3">
      <div class="card text-center">
        <div style="width: 60px; height: 60px; background: var(--gradient-primary); border-radius: var(--radius-full); display: flex; align-items: center; justify-content: center; margin: 0 auto var(--space-4);">
          <svg width="30" height="30" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2">
            <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
            <polyline points="22,6 12,13 2,6"/>
          </svg>
        </div>
        <h3 style="font-size: var(--text-xl); margin-bottom: var(--space-2);">이메일</h3>
        <p class="text-gray">contact@dlab.kr</p>
      </div>

      <div class="card text-center">
        <div style="width: 60px; height: 60px; background: var(--gradient-accent); border-radius: var(--radius-full); display: flex; align-items: center; justify-content: center; margin: 0 auto var(--space-4);">
          <svg width="30" height="30" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2">
            <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
          </svg>
        </div>
        <h3 style="font-size: var(--text-xl); margin-bottom: var(--space-2);">전화</h3>
        <p class="text-gray">02-1234-5678</p>
      </div>

      <div class="card text-center">
        <div style="width: 60px; height: 60px; background: var(--gradient-primary); border-radius: var(--radius-full); display: flex; align-items: center; justify-content: center; margin: 0 auto var(--space-4);">
          <svg width="30" height="30" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2">
            <circle cx="12" cy="12" r="10"/>
            <polyline points="12 6 12 12 16 14"/>
          </svg>
        </div>
        <h3 style="font-size: var(--text-xl); margin-bottom: var(--space-2);">운영 시간</h3>
        <p class="text-gray">평일 09:00 - 18:00<br>(주말 및 공휴일 휴무)</p>
      </div>
    </div>
  </div>
</section>

