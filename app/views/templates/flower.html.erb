<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Flower & Garden - Botanical Studio</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,600;1,400&family=Lato:wght@300;400&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Lato', sans-serif; color: #4a5d23; background-color: #fdfdfd; }
    h1, h2, h3, .font-serif { font-family: 'Playfair Display', serif; }
    .fade-in { animation: fadeIn 1.5s ease-out forwards; opacity: 0; }
    @keyframes fadeIn { to { opacity: 1; } }
  </style>
</head>
<body class="antialiased">

  <!-- Minimal Nav -->
  <nav class="fixed w-full z-50 bg-white/90 backdrop-blur-sm top-0 py-6 px-8 flex justify-between items-center border-b border-gray-100">
    <div class="text-2xl font-serif italic tracking-wider">Flower & Garden</div>
    <div class="hidden md:flex gap-10 text-xs font-light tracking-[0.2em] uppercase text-gray-500">
      <a href="#work" class="hover:text-green-800 transition">Our Work</a>
      <a href="#workshop" class="hover:text-green-800 transition">Workshop</a>
      <a href="#about" class="hover:text-green-800 transition">About</a>
    </div>
    <button onclick="toggleFlowerRes()" class="text-xs uppercase tracking-[0.2em] border-b border-gray-800 pb-1 hover:text-green-800 hover:border-green-800 transition">
      Order Flowers
    </button>
  </nav>

  <!-- Hero -->
  <header class="h-screen flex items-center justify-center relative overflow-hidden">
    <div class="absolute inset-x-0 bottom-0 h-1/2 bg-gradient-to-t from-green-50/50 to-transparent"></div>
    <div class="text-center z-10 p-8 fade-in">
       <p class="text-xs tracking-[0.4em] uppercase text-gray-400 mb-6">Floral Design Studio</p>
       <h1 class="text-5xl md:text-8xl font-serif font-light mb-8 text-[#2c3e10]">
         Nature's <br> Poetry
       </h1>
       <p class="max-w-md mx-auto text-gray-500 font-light leading-loose mb-12">
         We curate seasonal blooms to tell stories of love, celebration, and quiet moments.
       </p>
       <div class="w-px h-24 bg-gray-300 mx-auto"></div>
    </div>
    <!-- Floating Images -->
    <img src="https://images.unsplash.com/photo-1596003906949-6725008d38c9?q=80&w=600" class="absolute top-32 left-10 md:left-32 w-48 md:w-64 aspect-[3/4] object-cover shadow-xl fade-in" style="animation-delay: 0.5s">
    <img src="https://images.unsplash.com/photo-1542553457-377b752df78d?q=80&w=600" class="absolute bottom-32 right-10 md:right-32 w-56 md:w-80 aspect-[3/4] object-cover shadow-xl fade-in" style="animation-delay: 1s">
  </header>

  <!-- Gallery Grid -->
  <section id="work" class="py-32 px-6 max-w-7xl mx-auto">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 items-end">
      <div class="space-y-4 group cursor-pointer" onclick="toggleFlowerRes()">
        <div class="overflow-hidden">
           <img src="https://images.unsplash.com/photo-1563241527-30a6ec94c798?q=80&w=600" class="w-full aspect-[4/5] object-cover transition duration-700 group-hover:scale-105">
        </div>
        <h3 class="text-xl font-serif">Bridal Bouquet</h3>
        <p class="text-xs text-gray-400">WEDDING • 2024</p>
      </div>
      <div class="space-y-4 group cursor-pointer md:-translate-y-24" onclick="toggleFlowerRes()">
        <div class="overflow-hidden">
           <img src="https://images.unsplash.com/photo-1561181286-d3fee7d55364?q=80&w=600" class="w-full aspect-[4/5] object-cover transition duration-700 group-hover:scale-105">
        </div>
        <h3 class="text-xl font-serif">Table Centerpiece</h3>
        <p class="text-xs text-gray-400">DINNER • 2024</p>
      </div>
      <div class="space-y-4 group cursor-pointer" onclick="toggleFlowerRes()">
        <div class="overflow-hidden">
           <img src="https://images.unsplash.com/photo-1490216447820-2f3b97645068?q=80&w=600" class="w-full aspect-[4/5] object-cover transition duration-700 group-hover:scale-105">
        </div>
        <h3 class="text-xl font-serif">Seasonal Basket</h3>
        <p class="text-xs text-gray-400">GIFT • 2024</p>
      </div>
    </div>
  </section>

  <!-- Workshop -->
  <section id="workshop" class="py-32 bg-[#2c3e10] text-white">
    <div class="container mx-auto px-6 grid md:grid-cols-2 gap-16 items-center">
      <div class="order-2 md:order-1 relative">
         <img src="https://images.unsplash.com/photo-1516550893881-8b3877b068da?q=80&w=800" class="w-full h-[600px] object-cover opacity-80">
         <div class="absolute inset-0 border border-white/20 m-4"></div>
      </div>
      <div class="order-1 md:order-2 space-y-8">
        <span class="text-xs tracking-[0.2em] opacity-60 uppercase">One Day Class</span>
        <h2 class="text-4xl md:text-6xl font-serif leading-tight">Learn the Art <br> of Arrangement</h2>
        <p class="font-light opacity-70 leading-relaxed max-w-md">
          Join our intimate workshops where we guide you through selecting, conditioning, and arranging seasonal flowers.
        </p>
        <button onclick="toggleFlowerRes()" class="bg-white text-[#2c3e10] px-8 py-4 text-xs font-bold uppercase tracking-[0.2em] hover:bg-gray-100 transition">
          Book Workshop
        </button>
      </div>
    </div>
  </section>

  <!-- Reservation Modal -->
  <div id="flower-modal" class="fixed inset-0 bg-white/95 z-[60] hidden opacity-0 transition-opacity duration-500 flex items-center justify-center p-6">
    <div class="w-full max-w-2xl relative">
      <button onclick="toggleFlowerRes()" class="absolute -top-12 right-0 text-gray-400 hover:text-black text-sm tracking-widest uppercase">Close</button>
      
      <div class="grid md:grid-cols-2 gap-12">
        <div>
          <h2 class="text-4xl font-serif mb-6 text-[#2c3e10]">Inquiry</h2>
          <p class="text-gray-500 font-light mb-8 leading-relaxed">
            Please fill out the form for flower orders or workshop registration. We will get back to you within 24 hours.
          </p>
          <div class="space-y-4 text-sm text-gray-400">
             <p>t. 02-333-4444</p>
             <p>e. hello@flower.garden</p>
             <p>a. Hannam-dong, Seoul</p>
          </div>
        </div>
        
        <form class="space-y-8" onsubmit="event.preventDefault(); alert('Thank you. We will contact you shortly.'); toggleFlowerRes();">
          <div class="border-b border-gray-200">
            <input type="text" placeholder="Your Name" class="w-full py-4 bg-transparent outline-none font-serif text-lg placeholder-gray-300">
          </div>
          <div class="border-b border-gray-200">
            <input type="email" placeholder="Email Address" class="w-full py-4 bg-transparent outline-none font-serif text-lg placeholder-gray-300">
          </div>
          <div class="border-b border-gray-200">
            <select class="w-full py-4 bg-transparent outline-none font-serif text-lg text-gray-500">
              <option>Order Bouquet</option>
              <option>Wedding Inquiry</option>
              <option>Workshop Registration</option>
            </select>
          </div>
          <button class="w-full bg-[#2c3e10] text-white py-4 text-xs font-bold uppercase tracking-[0.2em] hover:opacity-90 transition mt-8">
            Send Message
          </button>
        </form>
      </div>
    </div>
  </div>

  <script>
    function toggleFlowerRes() {
      const modal = document.getElementById('flower-modal');
      if(modal.classList.contains('hidden')) {
        modal.classList.remove('hidden');
        setTimeout(() => modal.classList.remove('opacity-0'), 10);
      } else {
        modal.classList.add('opacity-0');
        setTimeout(() => modal.classList.add('hidden'), 500);
      }
    }
  </script>
</body>
</html>
