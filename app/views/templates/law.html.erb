<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>법무법인 정의 - 신뢰의 이름</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Garamond:ital,wght@0,400;0,700;1,400&family=Noto+Serif+KR:wght@300;400;500;700&display=swap" rel="stylesheet">
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            midnight: '#1a202c',
            gavel: '#8B4513',
            paper: '#f7fafc',
          },
          fontFamily: {
            serif: ['Garamond', 'serif'],
            kr: ['Noto Serif KR', 'serif'],
          }
        }
      }
    }
  </script>
</head>
<body class="bg-paper text-midnight font-kr">

  <!-- Nav -->
  <nav class="fixed w-full z-50 bg-white/95 backdrop-blur-sm border-b border-gray-200 shadow-sm">
    <div class="container mx-auto px-6 py-4 flex justify-between items-center">
      <div class="flex items-center gap-3">
        <div class="w-8 h-8 bg-midnight text-white flex items-center justify-center font-serif font-bold text-xl">L</div>
        <div class="font-bold text-xl tracking-tight">법무법인 <span class="text-gavel">정의</span></div>
      </div>
      <div class="hidden md:flex gap-10 font-medium text-gray-600">
        <a href="#about" class="hover:text-midnight transition">구성원 소개</a>
        <a href="#expert" class="hover:text-midnight transition">업무 분야</a>
        <a href="#result" class="hover:text-midnight transition">승소 사례</a>
      </div>
      <div class="flex gap-4">
        <span class="text-sm font-bold text-gavel self-center">Tel. 02-555-0000</span>
        <button onclick="toggleLaw()" class="bg-midnight text-white px-5 py-2 text-sm hover:bg-gray-800 transition">
          상담 예약
        </button>
      </div>
    </div>
  </nav>

  <!-- Hero Classy -->
  <header class="pt-32 pb-24 px-6 container mx-auto">
    <div class="grid md:grid-cols-2 gap-12 items-center">
      <div class="order-2 md:order-1 space-y-8 animate-fade-in-up md:pr-12">
        <div class="w-12 h-1 bg-gavel"></div>
        <h1 class="text-5xl md:text-6xl font-bold leading-tight">
          당신의 권리를 지키는 <br>
          <span class="italic text-gavel">든든한 법률 파트너</span>
        </h1>
        <p class="text-gray-600 text-lg leading-loose font-light">
          복잡하고 어려운 법률 문제, 혼자 고민하지 마십시오. <br>
          풍부한 경험과 전문성을 갖춘 변호인단이 <br>
          최적의 해결책을 제시해 드립니다.
        </p>
        <div class="flex gap-6 pt-4">
          <button onclick="toggleLaw()" class="border border-midnight px-8 py-3 hover:bg-midnight hover:text-white transition duration-300">
            무료 법률 상담
          </button>
          <button class="text-gray-500 hover:text-midnight border-b border-gray-300 hover:border-midnight pb-1 transition">
            오시는 길 &rarr;
          </button>
        </div>
      </div>
      <div class="order-1 md:order-2 relative h-[500px]">
        <div class="absolute top-0 right-0 w-3/4 h-full bg-gray-200 z-0"></div>
        <img src="https://images.unsplash.com/photo-1589829085413-56de8ae18c73?q=80&w=800" class="absolute bottom-12 right-12 w-3/4 h-4/5 object-cover shadow-2xl z-10 grayscale hover:grayscale-0 transition duration-700">
      </div>
    </div>
  </header>

  <!-- Practice Areas -->
  <section id="expert" class="py-24 bg-white border-t border-gray-100">
    <div class="container mx-auto px-6">
      <div class="text-center mb-16">
        <h2 class="text-3xl font-bold mb-4">전문 분야</h2>
        <p class="text-gray-500">각 분야 전문 변호사가 체계적으로 대응합니다.</p>
      </div>

      <div class="grid md:grid-cols-3 gap-8 text-center">
        <!-- Area 1 -->
        <div class="p-8 border border-gray-100 hover:border-gavel hover:shadow-lg transition duration-300 group cursor-pointer" onclick="toggleLaw()">
          <div class="text-gavel text-4xl mb-6">⚖️</div>
          <h3 class="text-xl font-bold mb-4">민사 소송</h3>
          <p class="text-gray-500 text-sm leading-relaxed">
            채권채무, 손해배상, 부동산 분쟁 등 <br> 재산상의 권리를 지켜드립니다.
          </p>
        </div>
        <!-- Area 2 -->
        <div class="p-8 border border-gray-100 hover:border-gavel hover:shadow-lg transition duration-300 group cursor-pointer" onclick="toggleLaw()">
           <div class="text-gavel text-4xl mb-6">🏢</div>
          <h3 class="text-xl font-bold mb-4">기업 법무</h3>
          <p class="text-gray-500 text-sm leading-relaxed">
            계약 검토, M&A, 기업 회생 등 <br> 기업 운영의 리스크를 관리합니다.
          </p>
        </div>
        <!-- Area 3 -->
        <div class="p-8 border border-gray-100 hover:border-gavel hover:shadow-lg transition duration-300 group cursor-pointer" onclick="toggleLaw()">
           <div class="text-gavel text-4xl mb-6">👨‍👩‍👧‍👦</div>
          <h3 class="text-xl font-bold mb-4">가사 / 이혼</h3>
          <p class="text-gray-500 text-sm leading-relaxed">
            이혼, 재산분할, 상속 등 <br> 가족 간의 분쟁을 원만하게 해결합니다.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Stats -->
  <section class="py-20 bg-midnight text-white text-center">
    <div class="container mx-auto px-6 grid md:grid-cols-3 gap-12 divide-x divide-white/20">
      <div>
        <div class="text-5xl font-serif mb-2">1,500+</div>
        <div class="text-sm tracking-widest uppercase text-gray-400">성공 사례</div>
      </div>
      <div>
        <div class="text-5xl font-serif mb-2">95%</div>
        <div class="text-sm tracking-widest uppercase text-gray-400">승소율</div>
      </div>
      <div>
        <div class="text-5xl font-serif mb-2">20+</div>
        <div class="text-sm tracking-widest uppercase text-gray-400">전문 변호사</div>
      </div>
    </div>
  </section>

  <!-- Modal -->
  <div id="law-modal" class="fixed inset-0 bg-midnight/80 z-[60] hidden flex items-center justify-center p-4">
    <div class="bg-white w-full max-w-lg p-12 relative shadow-xl">
      <button onclick="toggleLaw()" class="absolute top-6 right-6 text-gray-400 hover:text-black text-2xl">&times;</button>
      
      <div class="mb-10 border-b border-gray-200 pb-6">
        <h2 class="text-3xl font-bold text-midnight mb-2">법률 상담 예약</h2>
        <p class="text-sm text-gray-500">초기 상담은 15분간 무료로 진행됩니다.</p>
      </div>

      <form class="space-y-6" onsubmit="event.preventDefault(); alert('상담 예약이 접수되었습니다. 사무실에서 연락드리겠습니다.'); toggleLaw();">
        <div class="grid grid-cols-2 gap-6">
           <input type="text" placeholder="성함" class="w-full bg-gray-50 border border-gray-200 p-3 outline-none focus:border-midnight transition">
           <input type="tel" placeholder="연락처" class="w-full bg-gray-50 border border-gray-200 p-3 outline-none focus:border-midnight transition">
        </div>
        
        <select class="w-full bg-gray-50 border border-gray-200 p-3 outline-none focus:border-midnight transition text-gray-600">
          <option>상담 분야 선택</option>
          <option>민사 소송</option>
          <option>형사 소송</option>
          <option>이혼 / 가사</option>
          <option>기업 법무</option>
          <option>기타</option>
        </select>

        <textarea rows="4" placeholder="사건 개요를 간단히 적어주십시오. (비밀 보장)" class="w-full bg-gray-50 border border-gray-200 p-3 outline-none focus:border-midnight transition resize-none"></textarea>
        
        <button class="w-full bg-midnight text-white py-4 font-bold hover:bg-gavel transition duration-300">
          예약 신청
        </button>
      </form>
    </div>
  </div>

  <script>
    function toggleLaw() {
      const modal = document.getElementById('law-modal');
      if(modal.classList.contains('hidden')) {
        modal.classList.remove('hidden');
      } else {
        modal.classList.add('hidden');
      }
    }
  </script>
</body>
</html>
