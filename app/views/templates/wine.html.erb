<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>와인 소셜 - 도시의 밤</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Bodoni+Moda:ital,opsz,wght@0,6..96,400;0,6..96,600;1,6..96,500&family=Noto+Serif+KR:wght@300;400&display=swap" rel="stylesheet">
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            ruby: '#720e1e',
            void: '#0F0F0F',
            gold: '#C5A085',
          },
          fontFamily: {
            serif: ['Bodoni Moda', 'serif'],
            sans: ['Noto Serif KR', 'serif'],
          }
        }
      }
    }
  </script>
</head>
<body class="bg-void text-gray-400 font-sans">

  <!-- Nav -->
  <nav class="fixed w-full z-50 px-10 py-8 flex justify-between items-end mix-blend-difference">
    <div class="leading-none">
      <div class="text-xs uppercase tracking-[0.5em] mb-1 text-gold">Since 1999</div>
      <div class="text-3xl font-serif text-white uppercase tracking-widest">Wine Social</div>
    </div>
    <div class="flex gap-12 font-serif italic text-lg">
      <a href="#list" class="hover:text-gold transition">Wine List</a>
      <a href="#pairing" class="hover:text-gold transition">Food Pairing</a>
      <a href="#mood" class="hover:text-gold transition">Atmosphere</a>
    </div>
    <button onclick="toggleWine()" class="border-b border-gold text-gold hover:text-white transition pb-1 text-sm tracking-widest uppercase">
      Book Table
    </button>
  </nav>

  <!-- Parallax Hero -->
  <header class="h-screen relative overflow-hidden flex items-center justify-center">
    <div class="absolute inset-0 z-0">
      <img src="https://images.unsplash.com/photo-1510812431401-41d2bd2722f3?q=80&w=1920" class="w-full h-full object-cover opacity-30 scale-105 animate-slow-zoom">
    </div>
    
    <div class="relative z-10 text-center space-y-12">
      <div class="w-px h-24 bg-gradient-to-b from-transparent to-gold mx-auto"></div>
      <h1 class="text-6xl md:text-8xl font-serif text-white leading-tight">
        Uncork <br> <span class="italic text-ruby text-7xl md:text-9xl">The Night</span>
      </h1>
      <p class="max-w-md mx-auto leading-loose text-sm tracking-wide">
        엄선된 300여 종의 와인 컬렉션과 재즈 라이브. <br>
        깊어가는 밤, 당신의 감각을 깨우는 특별한 경험.
      </p>
      <div class="w-px h-24 bg-gradient-to-t from-transparent to-gold mx-auto"></div>
    </div>
  </header>

  <!-- Wine List Preview -->
  <section id="list" class="py-32 px-10 bg-void">
    <div class="max-w-5xl mx-auto">
      <div class="flex justify-between items-end mb-20 border-b border-white/10 pb-4">
        <h2 class="text-5xl font-serif text-white">Curated Selection</h2>
        <span class="text-gold italic">Sommelier's Pick</span>
      </div>

      <div class="grid md:grid-cols-2 gap-20">
        <!-- Item -->
        <div class="group cursor-pointer" onclick="toggleWine()">
          <div class="relative overflow-hidden mb-6 filter brightness-75 group-hover:brightness-100 transition duration-700">
             <img src="https://images.unsplash.com/photo-1559563362-c667ba5f5480?q=80&w=600" class="w-full aspect-[4/5] object-cover">
             <div class="absolute top-4 right-4 text-white font-serif text-xl border border-white/30 px-3 py-1 bg-black/50 backdrop-blur">96 pts</div>
          </div>
          <div class="flex justify-between items-baseline mb-2">
            <h3 class="text-2xl text-white font-serif">Château Margaux 2015</h3>
            <span class="text-gold">Glass ₩ 45,000</span>
          </div>
          <p class="text-sm leading-relaxed border-l border-ruby pl-4 text-gray-500">
            벨벳처럼 부드러운 탄닌과 풍부한 베리 향. <br> 보르도의 우아함을 완벽하게 표현한 빈티지.
          </p>
        </div>
        <!-- Item -->
        <div class="group cursor-pointer md:mt-32" onclick="toggleWine()">
           <div class="relative overflow-hidden mb-6 filter brightness-75 group-hover:brightness-100 transition duration-700">
             <img src="https://images.unsplash.com/photo-1584916201218-f4242ceb4809?q=80&w=600" class="w-full aspect-[4/5] object-cover">
             <div class="absolute top-4 right-4 text-white font-serif text-xl border border-white/30 px-3 py-1 bg-black/50 backdrop-blur">92 pts</div>
          </div>
          <div class="flex justify-between items-baseline mb-2">
            <h3 class="text-2xl text-white font-serif">Cloudy Bay Sauvignon Blanc</h3>
            <span class="text-gold">Glass ₩ 18,000</span>
          </div>
          <p class="text-sm leading-relaxed border-l border-ruby pl-4 text-gray-500">
            상큼한 라임과 열대과일의 폭발적인 아로마. <br> 해산물 요리와 완벽한 페어링을 자랑합니다.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Modal -->
  <div id="wine-modal" class="fixed inset-0 bg-black/90 z-[60] hidden flex items-center justify-center p-4">
    <div class="bg-void border border-white/10 w-full max-w-lg p-12 relative shadow-[0_0_50px_rgba(114,14,30,0.3)]">
      <button onclick="toggleWine()" class="absolute top-6 right-6 text-white hover:text-ruby text-2xl font-serif">X</button>
      
      <div class="text-center mb-12">
        <h2 class="font-serif text-4xl text-white mb-2 italic">Reserve</h2>
        <p class="text-xs text-gold uppercase tracking-[0.2em]">Private & Social</p>
      </div>

      <form class="space-y-8" onsubmit="event.preventDefault(); alert('예약 대기 신청이 완료되었습니다.'); toggleWine();">
        <div class="border-b border-white/20 pb-2">
           <input type="text" placeholder="성함" class="w-full bg-transparent outline-none text-white placeholder-gray-600 font-serif text-lg text-center">
        </div>
        <div class="border-b border-white/20 pb-2">
           <input type="tel" placeholder="연락처" class="w-full bg-transparent outline-none text-white placeholder-gray-600 font-serif text-lg text-center">
        </div>
        <div class="border-b border-white/20 pb-2">
           <select class="w-full bg-transparent outline-none text-gray-400 font-serif text-lg text-center">
             <option class="bg-void">Table for 2</option>
             <option class="bg-void">Table for 3-4</option>
             <option class="bg-void">Private Room (min. spend ₩300k)</option>
             <option class="bg-void">Bar Seat</option>
           </select>
        </div>
        
        <button class="w-full bg-ruby text-white py-4 font-serif text-xl tracking-widest hover:bg-white hover:text-ruby transition duration-500">
          Confirm
        </button>
      </form>
    </div>
  </div>

  <style>
    .animate-slow-zoom { animation: zoom 30s infinite alternate linear; }
    @keyframes zoom { from { transform: scale(1); } to { transform: scale(1.15); } }
  </style>

  <script>
    function toggleWine() {
      const modal = document.getElementById('wine-modal');
      if(modal.classList.contains('hidden')) {
        modal.classList.remove('hidden');
      } else {
        modal.classList.add('hidden');
      }
    }
  </script>
</body>
</html>
