<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Novus - Next Gen SaaS</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&family=JetBrains+Mono:wght@400;700&display=swap" rel="stylesheet">
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            bg: '#0B1120',
            surface: '#1E293B',
            primary: '#38BDF8',
            accent: '#818CF8',
          },
          fontFamily: {
            sans: ['Inter', 'sans-serif'],
            mono: ['JetBrains Mono', 'monospace'],
          }
        }
      }
    }
  </script>
  <style>
    body { background-color: #0B1120; color: #E2E8F0; }
    .bg-grid {
      background-size: 40px 40px;
      background-image: linear-gradient(to right, rgba(56, 189, 248, 0.05) 1px, transparent 1px),
                        linear-gradient(to bottom, rgba(56, 189, 248, 0.05) 1px, transparent 1px);
    }
  </style>
</head>
<body class="font-sans antialiased bg-grid overflow-x-hidden">

  <!-- Nav -->
  <nav class="fixed w-full z-50 px-6 py-4 border-b border-white/10 bg-bg/80 backdrop-blur-md">
    <div class="container mx-auto flex justify-between items-center">
      <div class="flex items-center gap-2 font-mono font-bold text-xl tracking-tight text-white">
        <div class="w-6 h-6 bg-gradient-to-br from-primary to-accent rounded"></div>
        NOVUS
      </div>
      <div class="hidden md:flex gap-8 text-sm font-medium text-gray-400">
        <a href="#features" class="hover:text-white transition">Features</a>
        <a href="#integration" class="hover:text-white transition">Integrations</a>
        <a href="#pricing" class="hover:text-white transition">Pricing</a>
      </div>
      <div class="flex gap-4">
        <button class="text-sm font-medium text-gray-400 hover:text-white">Log in</button>
        <button onclick="toggleStartup()" class="bg-white text-bg px-4 py-2 rounded-lg text-sm font-bold hover:bg-primary hover:text-white transition">
          Get Started
        </button>
      </div>
    </div>
  </nav>

  <!-- Hero -->
  <header class="relative pt-32 pb-20 px-6 overflow-hidden">
    <div class="container mx-auto text-center max-w-4xl relative z-10">
      <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-surface border border-white/10 text-xs font-mono text-primary mb-8">
        <span class="w-2 h-2 rounded-full bg-primary animate-pulse"></span> v2.0 is live
      </div>
      <h1 class="text-5xl md:text-7xl font-extrabold tracking-tight mb-6 bg-clip-text text-transparent bg-gradient-to-b from-white to-gray-400">
        Scale your infrastructure <br>
        without the <span class="text-primary">chaos.</span>
      </h1>
      <p class="text-lg text-gray-400 mb-10 max-w-2xl mx-auto leading-relaxed">
        Novus provides a unified control plane for your entire stack. 
        Monitor, deploy, and scale with a single CLI command.
      </p>
      
      <div class="flex flex-col md:flex-row gap-4 justify-center items-center mb-16">
        <div class="relative w-full md:w-auto">
          <input type="email" placeholder="Enter work email" class="w-full md:w-80 bg-surface border border-white/10 px-5 py-4 rounded-xl focus:outline-none focus:border-primary transition">
        </div>
        <button onclick="toggleStartup()" class="w-full md:w-auto px-8 py-4 bg-primary text-black font-bold rounded-xl hover:bg-white transition shadow-[0_0_20px_rgba(56,189,248,0.3)]">
          Start Free Trial
        </button>
      </div>

      <!-- Code Snippet -->
      <div class="bg-[#0f1623] border border-white/10 rounded-2xl p-6 text-left max-w-2xl mx-auto shadow-2xl font-mono text-sm overflow-hidden relative group">
        <div class="absolute top-4 right-4 flex gap-2">
          <div class="w-3 h-3 rounded-full bg-red-500"></div>
          <div class="w-3 h-3 rounded-full bg-yellow-500"></div>
          <div class="w-3 h-3 rounded-full bg-green-500"></div>
        </div>
        <div class="text-gray-500 mb-2"># Install Novus CLI</div>
        <div class="text-primary">$ npm install -g @novus/cli</div>
        <div class="text-gray-500 mt-4 mb-2"># Deploy your app</div>
        <div class="text-white">$ novus deploy --production</div>
        <div class="mt-2 text-emerald-400 group-hover:block hidden animate-pulse">
           > Building... <br>
           > Optimizing assets... <br>
           > Deployed successfully in 2.1s 🚀
        </div>
      </div>
    </div>
    
    <!-- Glow Effect -->
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[800px] bg-primary/10 rounded-full filter blur-[120px] -z-10"></div>
  </header>

  <!-- Features Grid -->
  <section id="features" class="py-24 px-6 border-t border-white/5 bg-surface/30">
    <div class="container mx-auto max-w-6xl">
       <div class="text-center mb-16">
         <h2 class="text-3xl font-bold mb-4">Everything you need to ship</h2>
         <p class="text-gray-400">Built for developers, by developers.</p>
       </div>
       
       <div class="grid md:grid-cols-3 gap-8">
         <div class="bg-surface border border-white/5 p-8 rounded-2xl hover:border-primary/50 transition duration-300">
           <div class="w-12 h-12 bg-primary/20 rounded-lg flex items-center justify-center text-primary mb-6">
             <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>
           </div>
           <h3 class="text-xl font-bold mb-3">Instant Deploy</h3>
           <p class="text-gray-400 text-sm leading-relaxed">Push to git and we care of the rest. Automated builds, previews, and production rollouts.</p>
         </div>
         <div class="bg-surface border border-white/5 p-8 rounded-2xl hover:border-primary/50 transition duration-300">
           <div class="w-12 h-12 bg-accent/20 rounded-lg flex items-center justify-center text-accent mb-6">
             <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/></svg>
           </div>
           <h3 class="text-xl font-bold mb-3">Enterprise Security</h3>
           <p class="text-gray-400 text-sm leading-relaxed">SOC2 compliant. Automated SSL, DDoS protection, and spicy waffles included.</p>
         </div>
         <div class="bg-surface border border-white/5 p-8 rounded-2xl hover:border-primary/50 transition duration-300">
           <div class="w-12 h-12 bg-purple-500/20 rounded-lg flex items-center justify-center text-purple-400 mb-6">
             <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-width="2" d="M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zM14 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zM14 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z"/></svg>
           </div>
           <h3 class="text-xl font-bold mb-3">Global Edge</h3>
           <p class="text-gray-400 text-sm leading-relaxed">Your app runs close to your users. 35+ regions worldwide with < 50ms latency.</p>
         </div>
       </div>
    </div>
  </section>

  <!-- Modal -->
  <div id="startup-modal" class="fixed inset-0 bg-bg/90 backdrop-blur-sm z-[60] hidden flex items-center justify-center p-4">
    <div class="bg-surface border border-white/10 w-full max-w-md rounded-2xl p-8 relative shadow-2xl">
      <button onclick="toggleStartup()" class="absolute top-4 right-4 text-gray-500 hover:text-white">&times;</button>
      
      <div class="text-center mb-8">
        <h2 class="text-2xl font-bold mb-2">Deploy in Seconds</h2>
        <p class="text-sm text-gray-400">No credit card required for hobby projects.</p>
      </div>

      <form class="space-y-4" onsubmit="event.preventDefault(); alert('Check your email for magic link!'); toggleStartup();">
        <div>
          <label class="block text-xs font-mono text-gray-500 mb-2">USERNAME</label>
          <input type="text" class="w-full bg-bg border border-white/10 p-3 rounded-lg text-white focus:outline-none focus:border-primary">
        </div>
        <div>
          <label class="block text-xs font-mono text-gray-500 mb-2">EMAIL ADDRESS</label>
          <input type="email" class="w-full bg-bg border border-white/10 p-3 rounded-lg text-white focus:outline-none focus:border-primary">
        </div>
        <div>
          <label class="block text-xs font-mono text-gray-500 mb-2">PASSWORD</label>
          <input type="password" class="w-full bg-bg border border-white/10 p-3 rounded-lg text-white focus:outline-none focus:border-primary">
        </div>
        
        <button class="w-full bg-gradient-to-r from-primary to-accent text-white py-4 rounded-lg font-bold hover:opacity-90 transition mt-6">
          Create Account
        </button>
      </form>
      
      <div class="mt-6 text-center text-xs text-gray-500">
        By clicking, you agree to our Terms and Privacy Policy.
      </div>
    </div>
  </div>

  <script>
    function toggleStartup() {
      const modal = document.getElementById('startup-modal');
      if(modal.classList.contains('hidden')) {
        modal.classList.remove('hidden');
      } else {
        modal.classList.add('hidden');
      }
    }
  </script>
</body>
</html>
