<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>젠틀 덴탈 - 편안한 미소</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&family=Noto+Sans+KR:wght@300;400;500;700&display=swap" rel="stylesheet">
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            teal: '#2DD4BF',
            sky: '#E0F2FE',
            blueGray: '#334155',
          },
          fontFamily: {
            display: ['DM Sans', 'sans-serif'],
            sans: ['Noto Sans KR', 'sans-serif'],
          }
        }
      }
    }
  </script>
</head>
<body class="bg-white text-blueGray font-sans">

  <!-- Nav -->
  <nav class="sticky top-0 z-50 bg-white/90 backdrop-blur-md border-b border-gray-100">
    <div class="container mx-auto px-6 py-4 flex justify-between items-center">
      <div class="text-2xl font-display font-bold text-teal flex items-center gap-2">
        <span class="text-3xl">🦷</span> GentleDental
      </div>
      <div class="hidden md:flex gap-8 font-medium text-sm text-gray-400">
        <a href="#about" class="hover:text-teal transition">병원 소개</a>
        <a href="#services" class="hover:text-teal transition">진료 과목</a>
        <a href="#reviews" class="hover:text-teal transition">치료 후기</a>
      </div>
      <button onclick="toggleDental()" class="bg-teal/10 text-teal px-5 py-2 rounded-lg font-bold hover:bg-teal hover:text-white transition">
        빠른 예약 (02-1234-5678)
      </button>
    </div>
  </nav>

  <!-- Hero with Split Layout -->
  <header class="grid md:grid-cols-2 min-h-screen">
    <div class="bg-teal flex items-center justify-center p-12 text-white relative overflow-hidden">
      <!-- Background Circles -->
      <div class="absolute top-0 left-0 w-64 h-64 bg-white/10 rounded-full blur-3xl -translate-x-1/2 -translate-y-1/2"></div>
      <div class="absolute bottom-0 right-0 w-96 h-96 bg-white/10 rounded-full blur-3xl translate-x-1/4 translate-y-1/4"></div>
      
      <div class="relative z-10 max-w-md space-y-6">
        <h1 class="text-5xl md:text-6xl font-display font-bold leading-tight">
          아프지 않은 치료, <br> 건강한 미소.
        </h1>
        <p class="text-teal-50 text-lg leading-relaxed">
          과잉 진료 없는 정직한 치과. <br>
          통증 최소화 시스템으로 편안한 진료를 약속합니다.
        </p>
        <div class="flex gap-4 pt-4">
          <button onclick="toggleDental()" class="bg-white text-teal px-8 py-4 rounded-xl font-bold shadow-lg hover:bg-gray-50 transition transform hover:-translate-y-1">
            진료 예약하기
          </button>
        </div>
      </div>
    </div>
    <div class="relative h-96 md:h-auto">
      <img src="https://images.unsplash.com/photo-1629909613654-28e377c37b09?q=80&w=1200" class="w-full h-full object-cover">
      <div class="absolute inset-0 bg-gradient-to-t from-white/50 to-transparent md:hidden"></div>
    </div>
  </header>

  <!-- Services Grid -->
  <section id="services" class="py-24 px-6 container mx-auto">
    <div class="text-center mb-16">
      <h2 class="text-3xl font-bold text-gray-800 mb-4">소중한 치아를 지켜드립니다</h2>
      <p class="text-gray-400">첨단 장비와 숙련된 의료진의 정밀한 진료</p>
    </div>

    <div class="grid md:grid-cols-4 gap-6">
      <!-- Card -->
      <div class="bg-sky/30 p-8 rounded-2xl hover:bg-sky/50 transition duration-300 group cursor-pointer" onclick="toggleDental()">
        <div class="bg-white w-14 h-14 rounded-xl flex items-center justify-center text-3xl mb-6 shadow-sm">🦷</div>
        <h3 class="text-xl font-bold mb-2">충치 치료</h3>
        <p class="text-sm text-gray-500 leading-relaxed">
          초기 진단부터 신경치료까지, 자연 치아를 최대한 보존합니다.
        </p>
      </div>
      <!-- Card -->
      <div class="bg-sky/30 p-8 rounded-2xl hover:bg-sky/50 transition duration-300 group cursor-pointer" onclick="toggleDental()">
        <div class="bg-white w-14 h-14 rounded-xl flex items-center justify-center text-3xl mb-6 shadow-sm">✨</div>
        <h3 class="text-xl font-bold mb-2">심미 보철</h3>
        <p class="text-sm text-gray-500 leading-relaxed">
          라미네이트, 올세라믹 등 티 나지 않는 자연스러운 아름다움.
        </p>
      </div>
      <!-- Card -->
      <div class="bg-sky/30 p-8 rounded-2xl hover:bg-sky/50 transition duration-300 group cursor-pointer" onclick="toggleDental()">
        <div class="bg-white w-14 h-14 rounded-xl flex items-center justify-center text-3xl mb-6 shadow-sm">🔩</div>
        <h3 class="text-xl font-bold mb-2">임플란트</h3>
        <p class="text-sm text-gray-500 leading-relaxed">
          3D CT 분석을 통한 안전하고 튼튼한 맞춤형 임플란트 식립.
        </p>
      </div>
      <!-- Card -->
      <div class="bg-sky/30 p-8 rounded-2xl hover:bg-sky/50 transition duration-300 group cursor-pointer" onclick="toggleDental()">
        <div class="bg-white w-14 h-14 rounded-xl flex items-center justify-center text-3xl mb-6 shadow-sm">😬</div>
        <h3 class="text-xl font-bold mb-2">치아 교정</h3>
        <p class="text-sm text-gray-500 leading-relaxed">
          소아 교정부터 성인 투명 교정까지, 가지런한 치열을 만듭니다.
        </p>
      </div>
    </div>
  </section>

  <!-- Review/Stats -->
  <section class="py-20 bg-gray-50">
    <div class="container mx-auto px-6 grid md:grid-cols-3 gap-12 text-center">
      <div>
        <div class="text-4xl font-bold text-teal mb-2">15,000+</div>
        <div class="text-gray-500 font-medium">누적 진료 환자</div>
      </div>
      <div>
        <div class="text-4xl font-bold text-teal mb-2">10년</div>
        <div class="text-gray-500 font-medium">한 자리에서 지켜온 신뢰</div>
      </div>
      <div>
         <div class="text-4xl font-bold text-teal mb-2">98%</div>
        <div class="text-gray-500 font-medium">환자 만족도</div>
      </div>
    </div>
  </section>

  <!-- Modal -->
  <div id="dental-modal" class="fixed inset-0 bg-blueGray/50 backdrop-blur-sm z-[60] hidden flex items-center justify-center p-4">
    <div class="bg-white w-full max-w-md p-8 rounded-2xl shadow-2xl relative">
      <button onclick="toggleDental()" class="absolute top-4 right-4 text-gray-400 hover:text-teal text-xl">&times;</button>
      
      <div class="text-center mb-8">
        <h2 class="text-2xl font-bold text-gray-800 mb-1">진료 예약</h2>
        <p class="text-xs text-gray-400">대기 시간 없이 편안하게 진료받으세요.</p>
      </div>

      <form class="space-y-4" onsubmit="event.preventDefault(); alert('예약 접수가 완료되었습니다.'); toggleDental();">
        <input type="text" placeholder="성함" class="w-full bg-gray-50 border border-gray-100 p-4 rounded-xl outline-none focus:border-teal transition text-sm font-bold">
        <input type="tel" placeholder="연락처" class="w-full bg-gray-50 border border-gray-100 p-4 rounded-xl outline-none focus:border-teal transition text-sm font-bold">
        
        <div class="grid grid-cols-2 gap-4">
          <input type="date" class="bg-gray-50 border border-gray-100 p-4 rounded-xl outline-none focus:border-teal transition text-sm text-gray-500">
          <input type="time" class="bg-gray-50 border border-gray-100 p-4 rounded-xl outline-none focus:border-teal transition text-sm text-gray-500">
        </div>
        
        <textarea rows="3" placeholder="불편하신 증상을 간단히 적어주세요." class="w-full bg-gray-50 border border-gray-100 p-4 rounded-xl outline-none focus:border-teal transition text-sm font-bold resize-none"></textarea>

        <button class="w-full bg-teal text-white py-4 rounded-xl font-bold shadow-lg hover:bg-teal-600 transition mt-2">
          예약 확정
        </button>
      </form>
    </div>
  </div>

  <script>
    function toggleDental() {
      const modal = document.getElementById('dental-modal');
      if(modal.classList.contains('hidden')) {
        modal.classList.remove('hidden');
      } else {
        modal.classList.add('hidden');
      }
    }
  </script>
</body>
</html>
