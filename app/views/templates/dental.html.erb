<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dental Care - Bright Smiles</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;600;700&display=swap" rel="stylesheet">
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            'dental-blue': '#0EA5E9',
            'dental-light': '#E0F2FE',
          },
          fontFamily: {
            sans: ['Plus Jakarta Sans', 'sans-serif'],
          }
        }
      }
    }
  </script>
</head>
<body class="bg-white text-slate-600 font-sans">

  <!-- Top Bar -->
  <div class="bg-dental-blue text-white py-2 text-center text-sm font-medium">
    Emergency? Call us 24/7: 02-123-4567
  </div>

  <!-- Nav -->
  <nav class="sticky top-0 z-50 bg-white/95 backdrop-blur-sm border-b border-slate-100">
    <div class="container mx-auto px-6 py-4 flex justify-between items-center">
      <div class="flex items-center gap-2 text-2xl font-bold text-slate-800">
        <svg class="w-8 h-8 text-dental-blue" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.828 14.828a4 4 0 01-5.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
        Dental Care
      </div>
      <div class="hidden md:flex gap-8 font-semibold">
        <a href="#services" class="hover:text-dental-blue">Treatments</a>
        <a href="#team" class="hover:text-dental-blue">Our Team</a>
        <a href="#reviews" class="hover:text-dental-blue">Reviews</a>
      </div>
      <button onclick="toggleDental()" class="bg-dental-blue text-white px-6 py-2.5 rounded-full font-bold hover:bg-blue-600 transition shadow-lg shadow-blue-200">
        Book Visit
      </button>
    </div>
  </nav>

  <!-- Hero with Split -->
  <header class="container mx-auto px-6 py-16 md:py-24 grid md:grid-cols-2 gap-12 items-center">
    <div class="space-y-6">
      <div class="inline-block px-4 py-1 bg-dental-light text-dental-blue rounded-full text-sm font-bold">
        New Patient Special: $99 Exam & X-Ray
      </div>
      <h1 class="text-5xl md:text-6xl font-bold text-slate-900 leading-tight">
        A Better Life Starts <br> With A <span class="text-dental-blue">Beautiful Smile.</span>
      </h1>
      <p class="text-lg leading-relaxed max-w-md">
        State-of-the-art technology met with gentle, personalized care. 
        Experience painless dentistry today.
      </p>
      <div class="flex gap-4 pt-4">
        <button onclick="toggleDental()" class="bg-slate-900 text-white px-8 py-4 rounded-xl font-bold hover:bg-slate-800 transition">Get Started</button>
        <button class="flex items-center gap-2 px-8 py-4 text-slate-600 font-bold hover:text-dental-blue">
          <svg class="w-5 h-5 bg-dental-blue text-white rounded-full p-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.752 11.168l-3.197-2.132A1 1 0 0010 9.87v4.263a1 1 0 001.555.832l3.197-2.132a1 1 0 000-1.664z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
          Watch Video
        </button>
      </div>
      <div class="pt-8 flex items-center gap-4 text-sm font-bold text-slate-400">
        <div class="flex -space-x-2">
           <div class="w-10 h-10 rounded-full bg-gray-200 border-2 border-white"></div>
           <div class="w-10 h-10 rounded-full bg-gray-300 border-2 border-white"></div>
           <div class="w-10 h-10 rounded-full bg-gray-400 border-2 border-white flex items-center justify-center text-white text-xs">+2k</div>
        </div>
        <div>Happy Patients</div>
      </div>
    </div>
    <div class="relative">
      <div class="absolute -inset-4 bg-dental-light rounded-[3rem] -z-10 transform rotate-3"></div>
      <img src="https://images.unsplash.com/photo-1606811971618-4486d14f3f72?q=80&w=800" class="w-full rounded-[2.5rem] shadow-2xl object-cover">
      <!-- Floating Badge -->
      <div class="absolute bottom-10 -left-6 bg-white p-6 rounded-2xl shadow-xl flex items-center gap-4 animate-bounce" style="animation-duration: 3s">
        <div class="bg-dental-light p-3 rounded-full text-dental-blue">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M5 13l4 4L19 7" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </div>
        <div>
          <div class="text-xs font-bold text-slate-400 uppercase">Success Rate</div>
          <div class="text-xl font-bold text-slate-900">99.8%</div>
        </div>
      </div>
    </div>
  </header>

  <!-- Services Grid -->
  <section id="services" class="py-24 bg-slate-50">
    <div class="container mx-auto px-6">
      <div class="text-center max-w-2xl mx-auto mb-16">
        <span class="text-dental-blue font-bold tracking-wider uppercase text-sm">Our Expertise</span>
        <h2 class="text-3xl md:text-4xl font-bold text-slate-900 mt-2">Comprehensive Dental Solutions</h2>
      </div>
      
      <div class="grid md:grid-cols-3 gap-8">
        <!-- Card -->
        <div class="bg-white p-8 rounded-3xl shadow-sm hover:shadow-xl hover:-translate-y-1 transition duration-300 cursor-pointer" onclick="toggleDental()">
          <div class="w-14 h-14 bg-blue-100 rounded-2xl flex items-center justify-center text-blue-600 mb-6 text-2xl">🦷</div>
          <h3 class="text-xl font-bold text-slate-900 mb-3">Implants</h3>
          <p class="text-slate-500 leading-relaxed">Restore your smile with permanent, natural-looking tooth replacements.</p>
        </div>
        <!-- Card -->
        <div class="bg-white p-8 rounded-3xl shadow-sm hover:shadow-xl hover:-translate-y-1 transition duration-300 cursor-pointer" onclick="toggleDental()">
          <div class="w-14 h-14 bg-teal-100 rounded-2xl flex items-center justify-center text-teal-600 mb-6 text-2xl">✨</div>
          <h3 class="text-xl font-bold text-slate-900 mb-3">Whitening</h3>
          <p class="text-slate-500 leading-relaxed">Brighten your teeth up to 8 shades in a single visiting session.</p>
        </div>
        <!-- Card -->
        <div class="bg-white p-8 rounded-3xl shadow-sm hover:shadow-xl hover:-translate-y-1 transition duration-300 cursor-pointer" onclick="toggleDental()">
          <div class="w-14 h-14 bg-purple-100 rounded-2xl flex items-center justify-center text-purple-600 mb-6 text-2xl">🛡️</div>
          <h3 class="text-xl font-bold text-slate-900 mb-3">Checkups</h3>
          <p class="text-slate-500 leading-relaxed">Routine examinations and cleaning to prevent future issues.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Reservation Modal -->
  <div id="dental-modal" class="fixed inset-0 bg-slate-900/50 backdrop-blur-sm z-[60] hidden flex items-center justify-center p-4">
    <div class="bg-white w-full max-w-md rounded-2xl shadow-2xl relative overflow-hidden">
      <div class="bg-dental-blue p-6 text-center">
        <h2 class="text-2xl font-bold text-white">Book Appointment</h2>
        <p class="text-blue-100 text-sm">We'll confirm your time shortly.</p>
      </div>
      <button onclick="toggleDental()" class="absolute top-4 right-4 text-white/70 hover:text-white">&times;</button>
      
      <div class="p-8">
        <form class="space-y-4" onsubmit="event.preventDefault(); alert('Booking Request Sent!'); toggleDental();">
          <div>
            <label class="font-bold text-xs text-slate-400 uppercase">Full Name</label>
            <input type="text" class="w-full border-2 border-slate-100 rounded-xl p-3 mt-1 focus:border-dental-blue outline-none transition font-bold text-slate-700">
          </div>
          <div>
            <label class="font-bold text-xs text-slate-400 uppercase">Reason for Visit</label>
            <select class="w-full border-2 border-slate-100 rounded-xl p-3 mt-1 focus:border-dental-blue outline-none transition font-bold text-slate-700">
              <option>General Checkup</option>
              <option>Tooth Pain</option>
              <option>Cleaning</option>
              <option>Whitening</option>
            </select>
          </div>
          <div class="grid grid-cols-2 gap-4">
             <div>
              <label class="font-bold text-xs text-slate-400 uppercase">Date</label>
              <input type="date" class="w-full border-2 border-slate-100 rounded-xl p-3 mt-1 focus:border-dental-blue outline-none transition font-bold text-slate-700">
            </div>
             <div>
              <label class="font-bold text-xs text-slate-400 uppercase">Phone</label>
              <input type="tel" class="w-full border-2 border-slate-100 rounded-xl p-3 mt-1 focus:border-dental-blue outline-none transition font-bold text-slate-700">
            </div>
          </div>
          
          <button class="w-full bg-slate-900 text-white font-bold py-4 rounded-xl mt-4 hover:bg-slate-800 transition shadow-lg">
            Confirm Booking
          </button>
        </form>
      </div>
    </div>
  </div>

  <script>
    function toggleDental() {
      const modal = document.getElementById('dental-modal');
      if(modal.classList.contains('hidden')) {
        modal.classList.remove('hidden');
      } else {
        modal.classList.add('hidden');
      }
    }
  </script>
</body>
</html>
