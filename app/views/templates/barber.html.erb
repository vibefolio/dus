<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>바버 클래식 - 신사의 공간</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Abril+Fatface&family=Rye&family=Noto+Sans+KR:wght@400;700&display=swap" rel="stylesheet">
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            leather: '#5D4037',
            gold: '#D4AF37',
            cream: '#F5F5DC',
          },
          fontFamily: {
            display: ['Rye', 'serif'],
            serif: ['Abril Fatface', 'cursive'],
            sans: ['Noto Sans KR', 'sans-serif'],
          }
        }
      }
    }
  </script>
</head>
<body class="bg-[#1a1a1a] text-cream font-sans">

  <!-- Nav -->
  <nav class="border-b border-gold/30 px-6 py-6 flex flex-col md:flex-row justify-between items-center gap-4">
    <div class="text-3xl font-display text-gold tracking-widest text-center md:text-left">
      BARBER CLASSIC
      <div class="text-xs font-sans text-gray-500 tracking-[0.5em] mt-1 opacity-70">EST. 2024</div>
    </div>
    <div class="flex gap-8 text-sm font-bold tracking-widest uppercase text-gray-400">
      <a href="#services" class="hover:text-gold transition">서비스</a>
      <a href="#about" class="hover:text-gold transition">스토리</a>
      <a href="#booking" class="hover:text-gold transition">예약</a>
    </div>
  </nav>

  <!-- Hero -->
  <header class="relative py-20 px-6 border-b-8 border-gold/10">
    <div class="container mx-auto grid md:grid-cols-2 gap-12 items-center">
      <div class="space-y-8 order-2 md:order-1 text-center md:text-left">
        <h1 class="text-5xl md:text-7xl font-display leading-tight text-white">
          Grooming for <br> <span class="text-gold shadow-gold">Gentlemen.</span>
        </h1>
        <p class="text-gray-400 text-lg leading-relaxed max-w-md mx-auto md:mx-0 font-serif">
          클래식한 감성과 섬세한 기술이 만나는 곳. <br>
          당신의 품격을 완성해 드립니다.
        </p>
        <button onclick="toggleBarber()" class="border-2 border-gold text-gold px-10 py-3 text-sm font-bold tracking-[0.2em] hover:bg-gold hover:text-black transition uppercase">
          예약하기
        </button>
      </div>
      <div class="order-1 md:order-2 relative">
        <div class="absolute inset-0 border-4 border-gold transform translate-x-4 translate-y-4"></div>
        <img src="https://images.unsplash.com/photo-1585747860715-2ba37e788b70?q=80&w=800" class="relative z-10 grayscale hover:grayscale-0 transition duration-700 w-full object-cover h-[500px]">
      </div>
    </div>
  </header>

  <!-- Services (Menu Style) -->
  <section id="services" class="py-24 bg-[#1f1f1f]">
    <div class="container mx-auto px-6 max-w-4xl">
      <div class="text-center mb-16">
        <span class="text-gold text-xs tracking-[0.3em] uppercase block mb-4">Price List</span>
        <h2 class="font-display text-4xl text-white">Our Services</h2>
      </div>

      <div class="grid md:grid-cols-2 gap-x-16 gap-y-8">
        <!-- Item -->
        <div class="border-b border-gray-700 pb-4 group cursor-pointer" onclick="toggleBarber()">
          <div class="flex justify-between items-end mb-2">
            <h3 class="font-serif text-2xl text-white group-hover:text-gold transition">Classic Cut</h3>
            <span class="font-bold text-gold text-xl">₩35,000</span>
          </div>
          <p class="text-xs text-gray-500 uppercase tracking-wider">가위질 & 스타일링 + 샴푸</p>
        </div>
        <!-- Item -->
        <div class="border-b border-gray-700 pb-4 group cursor-pointer" onclick="toggleBarber()">
          <div class="flex justify-between items-end mb-2">
            <h3 class="font-serif text-2xl text-white group-hover:text-gold transition">Hot Towel Shave</h3>
            <span class="font-bold text-gold text-xl">₩30,000</span>
          </div>
          <p class="text-xs text-gray-500 uppercase tracking-wider">전통 습식 면도 + 스팀 타월</p>
        </div>
        <!-- Item -->
        <div class="border-b border-gray-700 pb-4 group cursor-pointer" onclick="toggleBarber()">
          <div class="flex justify-between items-end mb-2">
            <h3 class="font-serif text-2xl text-white group-hover:text-gold transition">Beard Trim</h3>
            <span class="font-bold text-gold text-xl">₩20,000</span>
          </div>
          <p class="text-xs text-gray-500 uppercase tracking-wider">수염 다듬기 & 라인 정리</p>
        </div>
        <!-- Item -->
        <div class="border-b border-gray-700 pb-4 group cursor-pointer" onclick="toggleBarber()">
          <div class="flex justify-between items-end mb-2">
            <h3 class="font-serif text-2xl text-white group-hover:text-gold transition">Full Package</h3>
            <span class="font-bold text-gold text-xl">₩60,000</span>
          </div>
          <p class="text-xs text-gray-500 uppercase tracking-wider">컷트 + 쉐이빙 + 두피 마사지</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Modal -->
  <div id="barber-modal" class="fixed inset-0 bg-black/90 z-[60] hidden flex items-center justify-center p-4">
    <div class="bg-[#1a1a1a] border border-gold w-full max-w-md p-8 relative">
      <button onclick="toggleBarber()" class="absolute top-4 right-4 text-gray-500 hover:text-gold font-serif text-xl">X</button>
      
      <div class="text-center mb-8 border-b border-gold/20 pb-4">
        <h2 class="font-display text-2xl text-gold mb-2">Book Your Seat</h2>
        <p class="text-xs text-gray-500 uppercase tracking-widest">예약은 신사의 약속입니다</p>
      </div>

      <form class="space-y-6" onsubmit="event.preventDefault(); alert('예약이 확정되었습니다.'); toggleBarber();">
        <div>
           <label class="block text-xs font-bold text-gold uppercase mb-2 tracking-widest">Name</label>
           <input type="text" class="w-full bg-[#2a2a2a] border-b border-gray-600 p-2 text-white outline-none focus:border-gold transition font-serif">
        </div>
        <div>
           <label class="block text-xs font-bold text-gold uppercase mb-2 tracking-widest">Phone</label>
           <input type="tel" class="w-full bg-[#2a2a2a] border-b border-gray-600 p-2 text-white outline-none focus:border-gold transition font-serif">
        </div>
        <div class="grid grid-cols-2 gap-4">
           <div>
             <label class="block text-xs font-bold text-gold uppercase mb-2 tracking-widest">Date</label>
             <input type="date" class="w-full bg-[#2a2a2a] border-b border-gray-600 p-2 text-white outline-none focus:border-gold transition font-serif text-gray-400">
           </div>
           <div>
             <label class="block text-xs font-bold text-gold uppercase mb-2 tracking-widest">Time</label>
             <select class="w-full bg-[#2a2a2a] border-b border-gray-600 p-2 text-white outline-none focus:border-gold transition font-serif">
               <option>10:00 AM</option>
               <option>11:00 AM</option>
               <option>01:00 PM</option>
               <option>02:00 PM</option>
               <option>03:00 PM</option>
             </select>
           </div>
        </div>
        
        <button class="w-full bg-gold text-black font-bold py-4 text-xs tracking-[0.2em] hover:bg-white transition uppercase mt-4">
          Confirm Appointment
        </button>
      </form>
    </div>
  </div>

  <script>
    function toggleBarber() {
      const modal = document.getElementById('barber-modal');
      if(modal.classList.contains('hidden')) {
        modal.classList.remove('hidden');
      } else {
        modal.classList.add('hidden');
      }
    }
  </script>
</body>
</html>
