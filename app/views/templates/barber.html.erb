<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Gentleman's Cut - Classic Barber</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700;900&family=Lato:wght@300;400;700&display=swap" rel="stylesheet">
  <style>
    body { background-color: #1a1512; color: #d4af37; font-family: 'Lato', sans-serif; }
    .font-serif { font-family: 'Cinzel', serif; }
    .border-gold { border-color: #d4af37; }
    .text-gold { color: #d4af37; }
    .bg-gold { background-color: #d4af37; }
    .bg-leather { background-color: #2c241b; }
  </style>
</head>
<body class="antialiased overflow-x-hidden">

  <!-- Nav -->
  <nav class="fixed w-full z-50 bg-[#1a1512]/95 border-b border-gold/30 px-6 py-4 flex justify-between items-center">
    <div class="flex items-center gap-3">
       <!-- Barber Pole CSS Animation Placeholder -->
       <div class="w-4 h-10 bg-white overflow-hidden rounded-full border border-gray-600 relative">
         <div class="absolute inset-0 bg-[repeating-linear-gradient(45deg,#ff0000,#ff0000_5px,#ffffff_5px,#ffffff_10px,#0000ff_10px,#0000ff_15px,#ffffff_15px,#ffffff_20px)] animate-[spin_2s_linear_infinite]" style="animation-direction:reverse;"></div>
       </div>
       <div class="font-serif font-bold text-xl tracking-widest text-white">BARBER<span class="text-gold">CLASSIC</span></div>
    </div>
    <div class="hidden md:flex gap-8 text-xs font-bold tracking-[0.2em] uppercase text-white/70">
      <a href="#services" class="hover:text-gold transition">Services</a>
      <a href="#master" class="hover:text-gold transition">Master Barber</a>
      <a href="#location" class="hover:text-gold transition">Location</a>
    </div>
    <button onclick="toggleBarberRes()" class="border border-gold text-gold px-6 py-2 text-xs font-bold uppercase hover:bg-gold hover:text-black transition">
      Book Appointment
    </button>
  </nav>

  <!-- Hero -->
  <header class="relative h-screen flex items-center justify-center">
    <div class="absolute inset-0 z-0">
      <img src="https://images.unsplash.com/photo-1585747860715-2ba37e788b70?q=80&w=1920" class="w-full h-full object-cover opacity-40">
      <div class="absolute inset-0 bg-gradient-to-b from-[#1a1512]/50 via-transparent to-[#1a1512]"></div>
    </div>
    <div class="relative z-10 text-center max-w-4xl px-6 border-y-2 border-gold/50 py-12 bg-black/30 backdrop-blur-sm">
      <p class="text-sm tracking-[0.4em] uppercase text-white mb-6">Est. 1985</p>
      <h1 class="text-5xl md:text-8xl font-serif font-bold mb-6 text-white drop-shadow-xl">
        Sharp Looking <br> <span class="text-gold">Gentlemen</span>
      </h1>
      <p class="text-gray-300 mb-10 max-w-lg mx-auto leading-relaxed">
        Experience the art of traditional grooming. Hot towel shaves, precision cuts, and the finest whiskey while you wait.
      </p>
      <button onclick="toggleBarberRes()" class="bg-gold text-black px-10 py-4 font-bold tracking-[0.2em] hover:bg-white transition uppercase">
        Reserve Your Chair
      </button>
    </div>
  </header>

  <!-- Services (Menu Style) -->
  <section id="services" class="py-24 bg-leather relative">
    <div class="absolute inset-0 opacity-10" style="background-image: url('https://www.transparenttextures.com/patterns/leather.png');"></div>
    <div class="container mx-auto px-6 relative z-10 max-w-3xl">
      <div class="text-center mb-16">
        <h2 class="text-4xl font-serif font-bold text-white mb-4">Service Menu</h2>
        <div class="w-24 h-1 bg-gold mx-auto"></div>
      </div>
      
      <div class="space-y-8">
        <div class="flex justify-between items-baseline border-b border-white/10 pb-4">
          <div>
            <h3 class="text-2xl font-serif text-white">Classic Haircut</h3>
            <p class="text-sm text-gray-400 mt-1">Consultation, Cut, Wash, Style</p>
          </div>
          <div class="text-2xl font-bold text-gold">$45</div>
        </div>
        <div class="flex justify-between items-baseline border-b border-white/10 pb-4">
          <div>
            <h3 class="text-2xl font-serif text-white">Hot Towel Shave</h3>
            <p class="text-sm text-gray-400 mt-1">Straight Razor, Hot Towel, Face Massage</p>
          </div>
          <div class="text-2xl font-bold text-gold">$35</div>
        </div>
        <div class="flex justify-between items-baseline border-b border-white/10 pb-4">
          <div>
            <h3 class="text-2xl font-serif text-white">Beard Trim & Shape</h3>
            <p class="text-sm text-gray-400 mt-1">Sculpting, Oil Treatment</p>
          </div>
          <div class="text-2xl font-bold text-gold">$25</div>
        </div>
        <div class="flex justify-between items-baseline border-b border-white/10 pb-4">
          <div>
            <h3 class="text-2xl font-serif text-white">The Full Gentleman</h3>
            <p class="text-sm text-gray-400 mt-1">Haircut + Shave + Premium Whiskey</p>
          </div>
          <div class="text-2xl font-bold text-gold">$70</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Master Barber -->
  <section id="master" class="py-24 bg-[#15110e]">
    <div class="container mx-auto px-6 grid md:grid-cols-2 gap-12 items-center">
       <div class="relative">
         <div class="absolute inset-0 border-2 border-gold transform translate-x-4 translate-y-4"></div>
         <img src="https://images.unsplash.com/photo-1621605815971-fbc98d665033?q=80&w=800" class="relative z-10 w-full h-[500px] object-cover grayscale">
       </div>
       <div class="space-y-8">
         <h2 class="text-4xl font-serif font-bold text-white">Master Barber <br> <span class="text-gold">James Sterling</span></h2>
         <p class="text-gray-400 leading-relaxed text-lg">
           "A haircut is not just a service, it's an identity. For over 15 years, I've dedicated my life to the craft of traditional barbering, ensuring every gentleman leaves my chair feeling like a king."
         </p>
         <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/e/e4/Signature_sample.svg/1200px-Signature_sample.svg.png" class="h-16 invert opacity-50">
       </div>
    </div>
  </section>

  <!-- Footer (Location) -->
  <section id="location" class="py-20 text-center border-t border-white/10">
    <div class="font-serif text-2xl text-white mb-6">BARBER CLASSIC</div>
    <p class="text-gray-500 mb-2">123 Heritage Street, Old Town</p>
    <p class="text-gray-500 mb-8">Open Mon-Sat: 10am - 8pm</p>
    <div class="flex justify-center gap-6">
      <a href="#" class="text-gold hover:text-white transition">Instagram</a>
      <a href="#" class="text-gold hover:text-white transition">Facebook</a>
    </div>
  </section>

  <!-- Reservation Modal -->
  <div id="barber-modal" class="fixed inset-0 bg-black/95 z-[60] hidden opacity-0 transition-opacity duration-500 flex items-center justify-center p-6">
    <div class="w-full max-w-lg border border-gold p-8 md:p-12 relative bg-[#1a1512]">
      <button onclick="toggleBarberRes()" class="absolute top-4 right-4 text-gray-500 hover:text-gold text-2xl">&times;</button>
      
      <div class="text-center mb-10">
        <h2 class="text-3xl font-serif text-white mb-2">Secure Your Spot</h2>
        <div class="w-16 h-0.5 bg-gold mx-auto"></div>
      </div>

      <form class="space-y-6" onsubmit="event.preventDefault(); alert('Appointment Confirmed. See you soon, sir.'); toggleBarberRes();">
        <div class="grid md:grid-cols-2 gap-6">
           <input type="text" placeholder="Name" class="bg-transparent border-b border-gray-700 text-white py-3 focus:outline-none focus:border-gold">
           <input type="tel" placeholder="Phone" class="bg-transparent border-b border-gray-700 text-white py-3 focus:outline-none focus:border-gold">
        </div>
        <div class="py-4">
           <label class="block text-gray-500 text-xs uppercase mb-2">Service</label>
           <div class="flex gap-4">
             <label class="flex-1 cursor-pointer">
               <input type="radio" name="service" class="peer hidden" checked>
               <div class="border border-gray-700 py-3 text-center text-gray-400 peer-checked:border-gold peer-checked:text-gold transition text-sm">Haircut</div>
             </label>
             <label class="flex-1 cursor-pointer">
               <input type="radio" name="service" class="peer hidden">
               <div class="border border-gray-700 py-3 text-center text-gray-400 peer-checked:border-gold peer-checked:text-gold transition text-sm">Shave</div>
             </label>
             <label class="flex-1 cursor-pointer">
               <input type="radio" name="service" class="peer hidden">
               <div class="border border-gray-700 py-3 text-center text-gray-400 peer-checked:border-gold peer-checked:text-gold transition text-sm">Full</div>
             </label>
           </div>
        </div>
        <div>
           <label class="block text-gray-500 text-xs uppercase mb-2">Date & Time</label>
           <input type="datetime-local" class="w-full bg-transparent border-b border-gray-700 text-white py-3 focus:outline-none focus:border-gold">
        </div>
        <button class="w-full bg-gold text-black font-bold uppercase tracking-widest py-4 hover:bg-white transition mt-4">
          Confirm
        </button>
      </form>
    </div>
  </div>

  <script>
    function toggleBarberRes() {
      const modal = document.getElementById('barber-modal');
      if(modal.classList.contains('hidden')) {
        modal.classList.remove('hidden');
        setTimeout(() => modal.classList.remove('opacity-0'), 10);
      } else {
        modal.classList.add('opacity-0');
        setTimeout(() => modal.classList.add('hidden'), 500);
      }
    }
  </script>

</body>
</html>
