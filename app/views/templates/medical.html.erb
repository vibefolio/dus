<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pure Clinic - Advanced Dermatology</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,400&family=Montserrat:wght@300;400;500&display=swap" rel="stylesheet">
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            skin: '#FDF2F8',
            pure: '#FFFFFF',
            accent: '#DB2777',
          },
          fontFamily: {
            serif: ['Cormorant Garamond', 'serif'],
            sans: ['Montserrat', 'sans-serif'],
          }
        }
      }
    }
  </script>
</head>
<body class="bg-gray-50 font-sans text-gray-600">

  <!-- Nav -->
  <nav class="fixed w-full z-50 bg-white/80 backdrop-blur-md border-b border-gray-100 transition-all duration-300">
    <div class="container mx-auto px-6 py-4 flex justify-between items-center">
      <div class="font-serif text-3xl tracking-widest text-gray-800">
        PURE <span class="text-accent font-light">CLINIC</span>
      </div>
      <div class="hidden md:flex gap-10 text-xs font-medium tracking-[0.2em] uppercase text-gray-500">
        <a href="#procedures" class="hover:text-accent transition">Procedures</a>
        <a href="#specialists" class="hover:text-accent transition">Specialists</a>
        <a href="#reviews" class="hover:text-accent transition">Real Stories</a>
      </div>
      <button onclick="toggleMedical()" class="bg-black text-white px-6 py-2 text-xs font-bold tracking-widest hover:bg-accent transition">
        CONSULTATION
      </button>
    </div>
  </nav>

  <!-- Hero -->
  <header class="h-screen flex items-center justify-center relative overflow-hidden bg-skin">
    <div class="absolute inset-0 z-0">
      <img src="https://images.unsplash.com/photo-1616394584738-fc6e612e71b9?q=80&w=1920" class="w-full h-full object-cover opacity-50">
      <div class="absolute inset-0 bg-gradient-to-r from-skin via-skin/80 to-transparent"></div>
    </div>
    
    <div class="container mx-auto px-6 relative z-10 grid md:grid-cols-2">
      <div class="space-y-8">
        <p class="text-accent text-sm tracking-[0.3em] uppercase animate-fade-in-up">Esthetics & Dermatology</p>
        <h1 class="text-6xl md:text-8xl font-serif font-light text-gray-900 leading-none animate-fade-in-up" style="animation-delay: 0.2s">
          Reveal <br> Your <i class="text-accent">Glow</i>
        </h1>
        <p class="text-lg font-light leading-loose max-w-md animate-fade-in-up" style="animation-delay: 0.4s">
          Personalized skin solutions merging medical science with aesthetic artistry. 
          Experience the confidence of pure, radiant skin.
        </p>
        <div class="pt-8">
           <button onclick="toggleMedical()" class="border-b border-black pb-1 text-sm tracking-[0.2em] hover:text-accent hover:border-accent transition uppercase">Discover Treatments</button>
        </div>
      </div>
    </div>
  </header>

  <!-- Before / After (Concept) -->
  <section id="procedures" class="py-24 bg-white">
    <div class="container mx-auto px-6">
      <div class="text-center mb-20">
        <h2 class="font-serif text-4xl md:text-5xl text-gray-900 mb-6">Signature Treatments</h2>
        <div class="w-20 h-px bg-accent mx-auto"></div>
      </div>
      
      <div class="grid md:grid-cols-3 gap-12">
        <!-- Item 1 -->
        <div class="group cursor-pointer">
          <div class="overflow-hidden mb-6 relative">
            <img src="https://images.unsplash.com/photo-1596755389378-c31d21fd1273?q=80&w=600" class="w-full aspect-[3/4] object-cover transition duration-700 group-hover:scale-105">
            <div class="absolute inset-0 bg-black/0 group-hover:bg-black/10 transition duration-700"></div>
          </div>
          <h3 class="font-serif text-2xl mb-2 text-gray-900 group-hover:text-accent transition">Laser Toning</h3>
          <p class="text-sm font-light text-gray-500 leading-relaxed">Brighten skin tone and remove pigmentation with our dual-toning laser system.</p>
        </div>
        <!-- Item 2 -->
        <div class="group cursor-pointer mt-0 md:-mt-12">
          <div class="overflow-hidden mb-6 relative">
            <img src="https://images.unsplash.com/photo-1570172619644-dfd03ed5d881?q=80&w=600" class="w-full aspect-[3/4] object-cover transition duration-700 group-hover:scale-105">
             <div class="absolute inset-0 bg-black/0 group-hover:bg-black/10 transition duration-700"></div>
          </div>
          <h3 class="font-serif text-2xl mb-2 text-gray-900 group-hover:text-accent transition">Anti-Aging Filler</h3>
          <p class="text-sm font-light text-gray-500 leading-relaxed">Restore volume and smooth out fine lines naturally. FDA approved materials only.</p>
        </div>
        <!-- Item 3 -->
        <div class="group cursor-pointer">
          <div class="overflow-hidden mb-6 relative">
            <img src="https://images.unsplash.com/photo-1512496015851-a90fb38ba796?q=80&w=600" class="w-full aspect-[3/4] object-cover transition duration-700 group-hover:scale-105">
             <div class="absolute inset-0 bg-black/0 group-hover:bg-black/10 transition duration-700"></div>
          </div>
          <h3 class="font-serif text-2xl mb-2 text-gray-900 group-hover:text-accent transition">Acne Care</h3>
          <p class="text-sm font-light text-gray-500 leading-relaxed">Systematic 4-step program to control sebum and heal deep scars effectively.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Reservation Modal -->
  <div id="medical-modal" class="fixed inset-0 bg-white/95 z-[60] hidden flex items-center justify-center p-6 opacity-0 transition-opacity duration-500">
    <div class="max-w-2xl w-full text-center relative">
      <button onclick="toggleMedical()" class="absolute -top-12 right-0 text-3xl font-light hover:text-accent">&times;</button>
      
      <p class="text-accent text-xs tracking-[0.3em] uppercase mb-4">Book Appointment</p>
      <h2 class="font-serif text-5xl mb-12">Start Your Journey</h2>
      
      <form class="space-y-12" onsubmit="event.preventDefault(); alert('We will contact you to confirm.'); toggleMedical();">
        <div class="grid grid-cols-2 gap-12 border-b border-gray-200 pb-4">
           <input type="text" placeholder="Name" class="bg-transparent text-center text-xl outline-none placeholder-gray-300 font-serif">
           <input type="tel" placeholder="Phone" class="bg-transparent text-center text-xl outline-none placeholder-gray-300 font-serif">
        </div>
        <div class="border-b border-gray-200 pb-4">
           <select class="w-full bg-transparent text-center text-xl outline-none text-gray-500 font-serif">
             <option>Consultation</option>
             <option>Laser Treatment</option>
             <option>Skincare (Basic)</option>
             <option>Skincare (Premium)</option>
           </select>
        </div>
        
        <button class="bg-black text-white px-12 py-4 text-xs font-bold tracking-[0.2em] hover:bg-accent transition uppercase">
          Request Booking
        </button>
      </form>
    </div>
  </div>

  <script>
    function toggleMedical() {
      const modal = document.getElementById('medical-modal');
      if(modal.classList.contains('hidden')) {
        modal.classList.remove('hidden');
        setTimeout(() => modal.classList.remove('opacity-0'), 10);
      } else {
        modal.classList.add('opacity-0');
        setTimeout(() => modal.classList.add('hidden'), 500);
      }
    }
  </script>
</body>
</html>
