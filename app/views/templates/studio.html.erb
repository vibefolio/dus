<%# Studio Template - 모노크롬 (Monochrome) %>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Nanum+Myeongjo:wght@400;700;800&family=Noto+Sans+KR:wght@300;400;500;700;900&display=swap');
  
  .font-serif { font-family: 'Nanum Myeongjo', serif; }
  .font-sans { font-family: 'Noto Sans KR', sans-serif; }

  @media (max-width: 768px) {
    .mobile-desc { font-size: 14px !important; line-height: 1.6; }
    .mobile-title { font-size: 36px !important; }
  }
</style>

<div class="bg-black text-[#f2f2f2] font-sans selection:bg-white selection:text-black min-h-screen overflow-x-hidden">

  <!-- Nav -->
  <nav class="fixed w-full z-50 px-6 py-6 bg-black/9 worst backdrop-blur-md border-b border-white/5">
    <div class="container mx-auto flex justify-between items-center">
      <div class="flex items-center gap-4">
         <span class="font-serif text-2xl text-white tracking-[0.5em] font-black uppercase">MONO</span>
      </div>
      
      <div class="hidden md:flex gap-12 text-[10px] font-bold uppercase tracking-[0.4em] text-gray-600">
        <a href="#archives" class="hover:text-white transition">Archives</a>
        <a href="#philosophy" class="hover:text-white transition">Vision</a>
        <a href="#contact" class="hover:text-white transition">Booking</a>
      </div>
      
      <div class="flex gap-6 items-center">
         <button onclick="toggleStudioModal()" class="text-[10px] font-bold uppercase tracking-[0.4em] text-gray-600 hover:text-white">Client Archive</button>
         <button class="bg-white text-black px-6 py-2.5 rounded-sm font-black text-[10px] tracking-widest uppercase hover:bg-gray-200 transition shadow-xl">
           Schedule
         </button>
      </div>
    </div>
  </nav>

  <!-- Hero -->
  <header class="min-h-screen relative flex items-center justify-center overflow-hidden pt-20">
    <div class="absolute inset-0">
       <img src="https://images.unsplash.com/photo-1471341971476-84d5a7d74d82?q=80&w=1920" alt="스튜디오" class="w-full h-full object-cover grayscale opacity-20 brightness-50">
    </div>
    <div class="absolute inset-0 bg-gradient-to-b from-black via-transparent to-black"></div>
    
    <div class="container mx-auto px-6 relative z-10 text-center space-y-12">
      <div class="w-[1px] h-20 bg-white/30 mx-auto mb-10"></div>
      <p class="font-serif text-xl md:text-2xl italic tracking-[0.2em] text-gray-400">Capturing the Essence of Light</p>
      <h1 class="text-5xl md:text-9xl font-serif leading-none tracking-tighter text-white mobile-title">
         찰나의 정적,<br><span class="italic">영원한 잔상.</span>
      </h1>
      <p class="text-gray-600 text-sm md:text-xl leading-relaxed max-w-2xl mx-auto mobile-desc font-light">
         빛과 그림자, 그 사이의 가장 정직한 온도. 모노크롬은 인위적인 화려함 대신 <br>
         피사체 본연의 고유한 서사가 드러나는 찰나를 기록합니다.
      </p>
      <div class="pt-8">
         <a href="#archives" class="inline-flex items-center gap-6 text-white text-[10px] font-black uppercase tracking-[0.4em] group">
            See the Records <span class="group-hover:translate-x-2 transition">→</span>
         </a>
      </div>
    </div>
  </header>

  <!-- Login Modal -->
  <div id="studio-modal" class="fixed inset-0 z-[100] hidden items-center justify-center p-4 bg-white/5 worst backdrop-blur-md">
    <div class="bg-black w-full max-w-sm p-12 shadow-2xl relative border border-white/10 rounded-sm">
      <button onclick="toggleStudioModal()" class="absolute top-10 right-10 text-gray-500 hover:text-white transition text-2xl font-light">&times;</button>
      
      <div class="text-center mb-10">
        <h2 class="text-xl font-serif tracking-[0.4em] text-white mb-2 font-black uppercase">CLIENT_DATA</h2>
        <p class="text-[9px] text-gray-500 font-bold tracking-[0.3em] uppercase">Private Archive Access</p>
      </div>
      
      <div class="space-y-6">
        <input type="text" placeholder="ACCESS KEY" class="w-full bg-white/5 border border-white/5 py-5 px-6 outline-none focus:border-white/30 transition text-[11px] font-black tracking-widest text-white uppercase">
        <button class="w-full bg-white text-black py-5 rounded-sm text-[10px] tracking-[0.4em] font-black uppercase hover:bg-gray-200 transition shadow-lg">
           Authorize Stream
        </button>
        <div class="flex justify-center gap-10 pt-4">
           <a href="#" class="text-[9px] text-gray-500 tracking-[0.2em] font-bold uppercase hover:text-white transition">New Order</a>
           <a href="#" class="text-[9px] text-gray-500 tracking-[0.2em] font-bold uppercase hover:text-white transition">Support</a>
        </div>
      </div>
    </div>
  </div>

  <footer class="py-24 bg-black text-center px-6 border-t border-white/5">
     <div class="inline-flex items-center gap-6 mb-8">
        <span class="font-serif text-3xl tracking-[0.5em] font-black uppercase">MONO</span>
        <span class="w-12 h-[1px] bg-white/10"></span>
        <span class="text-[10px] font-bold text-gray-700 tracking-[0.4em] uppercase">Light & Shadow Archive</span>
     </div>
     <p class="text-[10px] text-white/5 tracking-[0.6em] uppercase px-6">© 2024 MONOCHROME STUDIO SEOUL. ALL RIGHTS RESERVED.</p>
  </footer>

  <script>
    function toggleStudioModal() {
      const modal = document.getElementById('studio-modal');
      modal.classList.toggle('hidden');
      modal.classList.toggle('flex');
    }
  </script>

</div>
