<div class="container mx-auto px-4 py-8">
  <div class="max-w-3xl mx-auto">
    <div class="mb-8">
      <h1 class="text-4xl font-bold text-gray-800 mb-2">FAQ 수정</h1>
      <p class="text-gray-600">자주 묻는 질문을 수정하세요.</p>
    </div>

    <div class="bg-white rounded-lg shadow-md p-8">
      <%= form_with model: [:admin, @faq], local: true, class: "space-y-6" do |f| %>
        
        <% if @faq.errors.any? %>
          <div class="bg-red-50 border-l-4 border-red-500 p-4 mb-6">
            <div class="flex">
              <div class="flex-shrink-0">
                <svg class="h-5 w-5 text-red-400" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
                </svg>
              </div>
              <div class="ml-3">
                <h3 class="text-sm font-medium text-red-800">
                  <%= pluralize(@faq.errors.count, "개의 오류") %>가 발생했습니다:
                </h3>
                <ul class="mt-2 text-sm text-red-700 list-disc list-inside">
                  <% @faq.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
            </div>
          </div>
        <% end %>

        <div>
          <%= f.label :question, "질문", class: "block text-sm font-medium text-gray-700 mb-2" %>
          <%= f.text_area :question, rows: 3, 
              class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 transition-colors",
              placeholder: "예: 구독을 중단하면 어떻게 되나요?" %>
        </div>

        <div>
          <%= f.label :answer, "답변", class: "block text-sm font-medium text-gray-700 mb-2" %>
          <%= f.text_area :answer, rows: 6, 
              class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 transition-colors",
              placeholder: "자세한 답변을 입력하세요..." %>
        </div>

        <div class="grid grid-cols-2 gap-6">
          <div>
            <%= f.label :position, "순서", class: "block text-sm font-medium text-gray-700 mb-2" %>
            <%= f.number_field :position, 
                class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 transition-colors",
                placeholder: "0" %>
            <p class="mt-1 text-sm text-gray-500">숫자가 작을수록 먼저 표시됩니다</p>
          </div>

          <div>
            <%= f.label :published, "공개 여부", class: "block text-sm font-medium text-gray-700 mb-2" %>
            <div class="mt-3">
              <%= f.check_box :published, class: "h-5 w-5 text-emerald-600 focus:ring-emerald-500 border-gray-300 rounded" %>
              <span class="ml-2 text-sm text-gray-700">공개</span>
            </div>
          </div>
        </div>

        <div class="flex gap-4 pt-6 border-t border-gray-200">
          <%= f.submit "수정 완료", class: "flex-1 bg-emerald-600 hover:bg-emerald-700 text-white font-bold py-3 px-6 rounded-lg transition-colors cursor-pointer" %>
          <%= link_to "취소", admin_faqs_path, class: "flex-1 bg-gray-200 hover:bg-gray-300 text-gray-800 font-bold py-3 px-6 rounded-lg transition-colors text-center" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
