<div style="max-width: 800px;">
  <div style="margin-bottom: var(--space-8);">
    <%= link_to "← 목록으로", admin_design_templates_path, style: "color: var(--color-gray-600); text-decoration: none;" %>
    <h1 style="font-size: var(--text-4xl); margin-top: var(--space-4);">새 템플릿 등록</h1>
  </div>
  <%= render "form", design_template: @design_template %>
</div>
