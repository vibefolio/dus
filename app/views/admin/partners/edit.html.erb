<div style="max-width: 800px; margin: 0 auto;">
  <div style="margin-bottom: var(--space-8);">
    <%= link_to "← 목록으로", admin_partners_path, style: "color: var(--color-gray-600); text-decoration: none;" %>
    <h1 style="font-size: var(--text-4xl); margin-top: var(--space-4); margin-bottom: var(--space-2);"><%= @partner.name %> 수정</h1>
  </div>
  <div class="card" style="padding: var(--space-8);">
    <%= render "form", partner: @partner %>
  </div>
</div>
